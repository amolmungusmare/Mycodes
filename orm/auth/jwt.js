// const jwt = require("jsonwebtoken");
// const data = { name: "Amol", sname: "Mungusmare" };
// const GenerateToken = (data) => {
//   const token = jwt.sign({ data }, "priya", { expiresIn: "1h" });

//   const accessToken = jwt.verify(token, "priya");
//   console.log(accessToken);
// };
// GenerateToken(data);
